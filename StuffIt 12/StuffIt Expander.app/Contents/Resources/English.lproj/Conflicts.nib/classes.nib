{
    IBClasses = (
        {
            ACTIONS = {replaceResolution = id; skipResolution = id; stopResolution = id; }; 
            CLASS = ConflictController; 
            LANGUAGE = ObjC; 
            OUTLETS = {applyToAllCheckbox = id; conflictDescription = id; window = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}